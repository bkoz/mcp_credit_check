apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: api-server
spec:
  source:
    type: Git
    git:
      uri: https://github.com/your-org/mcp_credit_check.git
      ref: main
    contextDir: api-server
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: Dockerfile
  output:
    to:
      kind: ImageStreamTag
      name: api-server:latest
  triggers:
    - type: ConfigChange
    - type: ImageChange
